<Window x:Class="AISPC.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AISPC - Система управления сборкой ПК" 
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen" 
        WindowState="Maximized"
        MinHeight="800" MinWidth="1400"
        Style="{StaticResource DarkWindow}">
    
    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#F0F6FC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#21262D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogoutButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DA3633"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F85149"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#B62324"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#161B22" Padding="20,15" BorderBrush="#30363D" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🖥️" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="AISPC - Система управления сборкой ПК" 
                               Foreground="#F0F6FC" FontSize="20" FontWeight="Bold"
                               VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock x:Name="UserInfoText" Text="" Foreground="#8B949E" 
                               VerticalAlignment="Center" Margin="0,0,15,0" FontSize="14"/>
                    <Button Content="🚪 Выход" Click="LogoutButton_Click"
                            Style="{StaticResource LogoutButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Основное содержимое -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Боковое меню -->
            <Border Grid.Column="0" Background="#0D1117" BorderBrush="#30363D" BorderThickness="0,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15,20">
                        <TextBlock Text="ОСНОВНЫЕ МОДУЛИ" Foreground="#8B949E" 
                                   FontSize="12" FontWeight="Bold" Margin="5,0,5,15"/>
                        
                        <Button Content="📊 Панель управления" Style="{StaticResource MenuButtonStyle}"
                                Click="MenuButton_Click" Tag="Dashboard"/>
                        <Button Content="🛠️ Каталог компонентов" Style="{StaticResource MenuButtonStyle}"
                                Click="MenuButton_Click" Tag="Components"/>
                        <Button Content="📋 Управление заказами" Style="{StaticResource MenuButtonStyle}"
                                Click="MenuButton_Click" Tag="Orders"/>
                        <Button Content="👥 Клиенты" Style="{StaticResource MenuButtonStyle}"
                                Click="MenuButton_Click" Tag="Clients"/>
                        
                        <TextBlock Text="ПРОИЗВОДСТВО" Foreground="#8B949E" 
                                   FontSize="12" FontWeight="Bold" Margin="5,25,5,15"/>
                        
                        <Button Content="🔨 Сборочные задания" Style="{StaticResource MenuButtonStyle}"
                                Click="MenuButton_Click" Tag="Assembly"/>
                        <Button Content="📦 Управление складом" Style="{StaticResource MenuButtonStyle}"
                                Click="MenuButton_Click" Tag="Warehouse"/>
                        
                        <TextBlock Text="АДМИНИСТРИРОВАНИЕ" Foreground="#8B949E" 
                                   FontSize="12" FontWeight="Bold" Margin="5,25,5,15"/>
                        
                        <Button Content="👤 Пользователи" Style="{StaticResource MenuButtonStyle}"
                                Click="MenuButton_Click" Tag="Users"/>
                        <Button Content="👷 Сотрудники" Style="{StaticResource MenuButtonStyle}"
                                Click="MenuButton_Click" Tag="Employees"/>
                        <Button Content="📈 Отчеты" Style="{StaticResource MenuButtonStyle}"
                                Click="MenuButton_Click" Tag="Reports"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            
            <!-- Рабочая область -->
            <Border Grid.Column="1" Background="#0D1117">
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
            </Border>
        </Grid>
        
        <!-- Статусная строка -->
        <Border Grid.Row="2" Background="#161B22" Padding="15,10" BorderBrush="#30363D" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusText" Text="Готов к работе" 
                           Foreground="#F0F6FC" VerticalAlignment="Center" FontSize="13"/>
                <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat='dd.MM.yyyy HH:mm'}" 
                           Foreground="#8B949E" VerticalAlignment="Center" FontSize="13"
                           xmlns:sys="clr-namespace:System;assembly=mscorlib"/>
            </Grid>
        </Border>
    </Grid>
</Window>