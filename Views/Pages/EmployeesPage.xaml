<Page x:Class="AISPC.Views.Pages.EmployeesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Сотрудники"
      Style="{StaticResource DarkPage}">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="👷 Управление сотрудниками" 
                   Style="{StaticResource DarkTitle}"/>
        
        <!-- Панель инструментов -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,25">
            <Button Content="➕ Добавить" Margin="0,0,15,0"
                    Style="{StaticResource DarkButton}"
                    Click="AddEmployee_Click"/>
            <Button Content="✏️ Изменить" Margin="0,0,15,0"
                    Style="{StaticResource DarkButton}"
                    Click="EditEmployee_Click"/>
            <Button Content="📋 Карточка" Margin="0,0,15,0"
                    Style="{StaticResource DarkButton}"
                    Click="ShowEmployeeCard_Click"/>
            <Button Content="💰 Зарплата"
                    Style="{StaticResource DarkButton}"
                    Click="ShowSalary_Click"/>
        </StackPanel>
        
        <!-- Таблица сотрудников -->
        <Border Grid.Row="2" Style="{StaticResource DarkBorder}">
            <DataGrid x:Name="EmployeesGrid" 
                      Style="{StaticResource DarkDataGrid}"
                      ColumnHeaderStyle="{StaticResource DarkDataGridColumnHeader}"
                      RowStyle="{StaticResource DarkDataGridRow}"
                      CellStyle="{StaticResource DarkDataGridCell}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="250"/>
                    <DataGridTextColumn Header="Должность" Binding="{Binding Position}" Width="200"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="150"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Дата приема" Binding="{Binding HireDate, StringFormat=dd.MM.yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Зарплата" Binding="{Binding Salary, StringFormat=C}" Width="120"/>
                    <DataGridTextColumn Header="Активен" Binding="{Binding IsActive}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>